# Booking Backend REST API
 
## Only first time npm init -y

### installations untill now:

- npm install morgan --save 
- npm install mongoose --save
- npm install body-parser --save
- npm install morgan --save
- npm install --save express-validator

